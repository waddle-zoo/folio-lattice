# fl-urj.17 360px focus/state evidence

- Source base: `da19a21a8ebe936e91f1573b5ef788efc377618e`
- Browser: Google Chrome `153.0.8010.48`
- Scope: human library route at 360×800, keyboard focus, live busy state, and
  control bounds.

## Commands and results

```text
FOLIO_BROWSER="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
PYTHONPATH=src uv run pytest -q tests/test_browser_e2e.py \
  -k real_ui_upload_search_grep_chunk_graph_versions_conflict_and_bridge
1 passed, 5 deselected

FOLIO_BROWSER="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
PYTHONPATH=src uv run pytest -q tests/test_browser_e2e.py
6 passed

PYTHONPATH=src uv run pytest -q tests/test_web.py tests/test_service.py
37 passed, 9 subtests passed

uv run ruff check tests/test_browser_e2e.py
uv run ruff format --check tests/test_browser_e2e.py
git diff --check
pass
```

The browser assertions verify no horizontal overflow at 360px, every visible
form control stays within the viewport, the native Search-library disclosure
opens with focus on its query field, and `main[aria-busy]` transitions from
`true` during a delayed real MCP search back to `false` after six results load.

This is automated browser/accessibility-tree evidence. It does not claim an
independent VoiceOver or other real screen-reader witness; that remains a
separate release-gate requirement.

No enterprise-readiness claim.
